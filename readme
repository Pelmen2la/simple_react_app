Как запустить проект:

1. Качаем репо.
2. В папке репо выполняешь npm install.
3. В папку server/config создаем файл app-config.json с таким содержимым.
{
    "appPort": Number - порт на котром будет крутиться приложение, например 3000
}
4. В папке репо выполняем npm run webpackprod - соберется бандл для продакшена
    или npm run webpackwatch соберется бандл для разработки и процесс будет наблюдать за изменениями в коде и пересобирать бандл.
5. В папке репо выполнить npm run startapp - приложуха запустится на localhost-е на порте указанном в конфиге.

!! WARNING !!

Если после пула не собирается веб, попробуйте выполнить npm install, возможно изменились зависимости.